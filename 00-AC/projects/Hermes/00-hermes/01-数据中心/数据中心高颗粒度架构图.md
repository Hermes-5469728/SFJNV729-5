
# 数据中心高颗粒度架构图

## 概述

本文档详细描述 TRAE 项目数据中心的高颗粒度架构设计，涵盖知识库层、业务数据层、配置层的完整结构和交互关系。

---

## 一、数据中心整体架构

```mermaid
graph TD
    subgraph 数据中心 [Data Center]
        subgraph 知识库层 [Hermes - 知识库层]
            H1[原始文档存储]
            H2[向量索引存储]
            H3[知识分类管理]
        end
        
        subgraph 业务数据层 [Data - 业务数据层]
            D1[结构化数据库]
            D2[文件附件存储]
            D3[应用日志]
        end
        
        subgraph 配置层 [Config - 配置层]
            C1[应用配置]
            C2[环境变量]
            C3[多环境配置]
        end
    end
    
    subgraph 核心应用层 [Core Application]
        A1[API网关]
        A2[业务模块]
        A3[数据访问层]
        A4[向量检索引擎]
    end
    
    subgraph 外部访问 [External Access]
        E1[Web客户端]
        E2[移动客户端]
        E3[第三方集成]
    end
    
    E1 --> A1
    E2 --> A1
    E3 --> A1
    
    A1 --> A2
    A2 --> A3
    A2 --> A4
    
    A3 --> D1
    A3 --> D2
    A3 --> D3
    
    A4 --> H1
    A4 --> H2
    
    A2 --> C1
    A2 --> C2
```

---

## 二、知识库层（Hermes）架构

### 2.1 目录结构

```mermaid
graph TD
    subgraph Hermes [Hermes/ - 知识库根目录]
        subgraph 元数据 [00-hermes/]
            M1[项目秘书.md]
            M2[架构设计/]
            M3[数据中心/]
        end
        
        subgraph 原始文档 [raw/]
            R1[medical/ - 医学资料]
            R2[technical/ - 技术文档]
            R3[personal/ - 个人笔记]
        end
        
        subgraph 向量索引 [vector_store/]
            V1[medical.index/ - 医学向量库]
            V2[general.index/ - 通用向量库]
        end
    end
    
    M1 --> M2
    M1 --> M3
    R1 --> V1
    R2 --> V2
    R3 --> V2
```

### 2.2 组件职责

| 组件 | 职责 | 存储内容 | 访问方式 |
|------|------|----------|----------|
| **00-hermes/** | 元数据管理 | 架构文档、项目配置 | 直接读取 |
| **raw/medical/** | 医学知识存储 | 医学笔记、临床指南 | 文件系统 |
| **raw/technical/** | 技术文档存储 | API文档、架构说明 | 文件系统 |
| **raw/personal/** | 个人笔记存储 | 经验总结、避坑指南 | 文件系统 |
| **vector_store/** | 向量索引存储 | 语义检索索引 | 向量数据库API |

---

## 三、业务数据层（Data）架构

### 3.1 目录结构

```mermaid
graph TD
    subgraph Data [data/ - 业务数据根目录]
        subgraph 结构化数据 [sqlite/]
            S1[dads_personal.db]
            S2[dads_medical.db]
            S3[ac_core.db]
        end
        
        subgraph 文件存储 [files/]
            F1[attachments/ - 附件]
            F2[reports/ - 报告]
            F3[backups/ - 备份]
        end
        
        subgraph 日志 [logs/]
            L1[application.log]
            L2[error.log]
        end
    end
    
    S1 --> F1
    S2 --> F1
    S3 --> F1
    S1 --> F2
    S2 --> F2
```

### 3.2 数据库职责

| 数据库 | 所属模块 | 存储内容 | 表前缀 |
|--------|----------|----------|--------|
| **dads_personal.db** | DADS个人版 | 合规记录、沟通留痕、工作日志 | `personal_` |
| **dads_medical.db** | DADS医疗版 | 诊断数据、评分系统、临床决策 | `medical_` |
| **ac_core.db** | AC决策中心 | 用户配置、任务记录、分析结果 | `ac_` |

### 3.3 文件存储分类

| 目录 | 职责 | 文件类型 | 生命周期 |
|------|------|----------|----------|
| **attachments/** | 沟通附件存储 | PDF、图片、音频 | 长期保存 |
| **reports/** | 生成报告存储 | PDF、Excel | 定期清理 |
| **backups/** | 数据备份存储 | SQL备份、压缩包 | 按策略保留 |

---

## 四、配置层（Config）架构

### 4.1 目录结构

```mermaid
graph TD
    subgraph Config [config/ - 配置根目录]
        C1[settings.yaml - 应用配置]
        C2[.env - 环境变量]
        
        subgraph 多环境配置 [profiles/]
            P1[development.yaml]
            P2[testing.yaml]
            P3[production.yaml]
        end
    end
    
    C1 --> P1
    C1 --> P2
    C1 --> P3
```

### 4.2 配置文件职责

| 文件 | 职责 | 存储内容 | 安全性 |
|------|------|----------|--------|
| **settings.yaml** | 应用基础配置 | 路径、端口、日志级别 | 非敏感 |
| **.env** | 环境敏感配置 | 数据库密码、API密钥 | 敏感，不提交Git |
| **profiles/*.yaml** | 多环境配置 | 各环境特有参数 | 根据环境决定 |

---

## 五、数据流动与交互

### 5.1 数据读写流程

```mermaid
sequenceDiagram
    participant Client as 客户端
    participant API as API网关
    participant Service as 业务服务
    participant DAL as 数据访问层
    participant SQLite as SQLite数据库
    participant Files as 文件存储
    participant Vector as 向量索引
    
    %% 结构化数据读取
    Client->>API: 请求业务数据
    API->>Service: 处理业务逻辑
    Service->>DAL: 查询数据
    DAL->>SQLite: SELECT查询
    SQLite-->>DAL: 返回结果
    DAL-->>Service: 数据对象
    Service-->>API: 响应数据
    API-->>Client: JSON响应
    
    %% 文件上传
    Client->>API: 上传附件
    API->>Service: 处理文件
    Service->>Files: 保存文件
    Files-->>Service: 文件路径
    Service->>DAL: 记录文件信息
    DAL->>SQLite: INSERT记录
    SQLite-->>DAL: 成功
    DAL-->>Service: 记录ID
    Service-->>API: 上传结果
    API-->>Client: 成功响应
    
    %% 知识检索
    Client->>API: 语义检索请求
    API->>Service: 检索服务
    Service->>Vector: 查询向量
    Vector-->>Service: 匹配结果
    Service->>DAL: 获取详情
    DAL->>SQLite: 查询元数据
    SQLite-->>DAL: 元数据
    DAL-->>Service: 完整信息
    Service-->>API: 检索结果
    API-->>Client: 匹配文档列表
```

### 5.2 数据同步机制

```mermaid
graph TD
    subgraph 数据同步流程
        A[原始文档更新] --> B[触发向量更新]
        B --> C[生成新向量]
        C --> D[更新向量索引]
        D --> E[标记同步完成]
        
        F[数据库变更] --> G[记录变更日志]
        G --> H[触发备份任务]
        H --> I[生成备份文件]
        I --> J[存储到backups/]
    end
```

---

## 六、路径配置与跨平台兼容

### 6.1 配置代码结构

```mermaid
graph TD
    subgraph config.py [src/core/config.py]
        A[ROOT_DIR - 项目根目录]
        B[HermesPaths - 知识库路径]
        C[DataPaths - 业务数据路径]
        D[ConfigPaths - 配置路径]
        E[DatabaseConfig - 数据库配置]
        F[ensure_directories - 目录初始化]
    end
    
    A --> B
    A --> C
    A --> D
    B --> F
    C --> F
    D --> F
    C --> E
```

### 6.2 路径定义示例

```python
# 核心路径定义逻辑
ROOT_DIR = Path(__file__).resolve().parent.parent.parent

class DataPaths:
    BASE = ROOT_DIR / "data"
    SQLITE = BASE / "sqlite"
    FILES = BASE / "files"
    LOGS = BASE / "logs"
```

---

## 七、安全与权限设计

```mermaid
graph TD
    subgraph 安全边界
        A[外部访问层]
        B[API网关]
        C[业务服务层]
        D[数据访问层]
        E[数据存储层]
    end
    
    A -->|认证| B
    B -->|授权| C
    C -->|权限校验| D
    D -->|访问控制| E
    
    subgraph 敏感数据保护
        F[.env文件]
        G[数据库密码]
        H[API密钥]
    end
    
    F -->|加密存储| G
    F -->|加密存储| H
    style F fill:#f9f,stroke:#333,stroke-width:2px
    style G fill:#f9f,stroke:#333,stroke-width:2px
    style H fill:#f9f,stroke:#333,stroke-width:2px
```

---

## 八、扩展与演进规划

### 8.1 短期规划（0-6个月）

| 目标 | 描述 | 优先级 |
|------|------|--------|
| 向量数据库集成 | 接入专业向量数据库（如Pinecone、Milvus） | 高 |
| 备份自动化 | 实现定时自动备份 | 高 |
| 日志管理 | 集成ELK或类似日志系统 | 中 |

### 8.2 长期规划（6-18个月）

| 目标 | 描述 | 优先级 |
|------|------|--------|
| 分布式存储 | 引入对象存储（如MinIO） | 中 |
| 多数据库支持 | 支持PostgreSQL、MySQL | 中 |
| 数据同步 | 实现跨设备数据同步 | 低 |

---

## 九、架构优势总结

| 优势 | 说明 |
|------|------|
| **分层清晰** | 知识库、业务数据、配置三层分离，职责明确 |
| **模块隔离** | 各模块独立数据库，避免数据混杂 |
| **安全设计** | 敏感配置独立管理，不提交版本控制 |
| **跨平台兼容** | 使用pathlib实现路径统一处理 |
| **可扩展性** | 预留向量数据库、分布式存储扩展空间 |

---

*文档版本: v1.0*  
*创建时间: 2026年5月*  
*所属项目: TRAE数据中心架构*
